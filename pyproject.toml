[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "siwi"
version = "0.0.0"
description = "Lightweight RAG ChatBot backend with optional graph experiments."
requires-python = ">=3.10"
dependencies = []
